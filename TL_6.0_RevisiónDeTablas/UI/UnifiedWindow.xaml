<Window x:Class="TL60_RevisionDeTablas.UI.UnifiedWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TL 6.0 - Auditoría Unificada"
        Height="750" Width="1650"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="TabStyle" TargetType="TabItem">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Background" Value="#ECF0F1"/>
            <Setter Property="BorderThickness" Value="0,0,0,3"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="ContentSite"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center"
                                              ContentSource="Header"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="White"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#3498DB"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#BDC3C7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <!-- TabControl con los plugins -->
        <TabControl x:Name="PluginsTabControl"
                    BorderThickness="0" Background="White">

            <!-- Pestaña 1: Auditoría de Parámetros de Metrados -->
            <TabItem Header="Parámetros de Metrados" Style="{StaticResource TabStyle}">
                <ContentControl x:Name="MetradosPluginContainer"/>
            </TabItem>

            <!-- Pestaña 2: Auditoría de Parámetros COBie -->
            <TabItem Header="Parámetros COBie" Style="{StaticResource TabStyle}">
                <ContentControl x:Name="CobiePluginContainer"/>
            </TabItem>

            <!-- Pestaña 3: Auditoría de Parámetros Uniclass -->
            <TabItem Header="Parámetros Uniclass" Style="{StaticResource TabStyle}">
                <ContentControl x:Name="UniclassPluginContainer"/>
            </TabItem>

            <!-- Pestaña 4: Auditoría de Tablas de Planificación -->
            <TabItem Header="Tablas de Planificación" Style="{StaticResource TabStyle}">
                <ContentControl x:Name="TablasPluginContainer"/>
            </TabItem>

            <!-- Pestaña 5: Auditoría de Project Browser -->
            <TabItem Header="Project Browser" Style="{StaticResource TabStyle}">
                <ContentControl x:Name="ProjectBrowserPluginContainer"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
